__pycache__/
*.pyc
.pytest_cache/
.venv

# Build artifacts from `pip install -e` (auto-generated, not source)
# Artifact build do `pip install -e` sinh ra (tự sinh, không phải mã nguồn)
*.egg-info/

# Runtime secrets - generated by tools/generate_dev_certs.py or your Trust Service.
# We keep runtime/security/README.md in git (setup guide) but never the secrets.
# Bí mật runtime - sinh bởi tools/generate_dev_certs.py hoặc Trust Service của bạn.
# Giữ runtime/security/README.md trong git (hướng dẫn setup), KHÔNG commit bí mật.
**/runtime/security/cert.json
**/runtime/security/*.pem
**/runtime/security/bootstrap.txt

# Internal framework-issue notes - not part of the public example.
# Ghi chú vấn đề framework nội bộ - không thuộc ví dụ công khai.
van-de-framework/
